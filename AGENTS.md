# Repository State

- This repository is currently a scaffold: it has no source tree, dependency manifest, tool configuration, CI, or verified development commands.
- Do not infer a Python stack from `.gitignore`; it is only a generic Python template. Re-check the repository before choosing tools or architecture.

# Workflow

- Always load and follow the `tdd` skill before implementing features, fixing bugs, or changing production code. Work test-first (red, green, refactor).
- Do not invent setup, build, lint, typecheck, or test commands. Add them here only after executable project configuration exists and the commands have been verified.
